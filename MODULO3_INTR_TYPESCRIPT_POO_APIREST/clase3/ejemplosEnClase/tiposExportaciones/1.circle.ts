// aqui usaremos la exportacion directa para la siguiente funcion
// y la constante

export const calculateCircleArea = (radius: number): number => {
    return Math.PI * radius * radius
}

export const CIRCLE_NAME = "Circle"